<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/WidgetLayout"
    style="@style/Widget.MindMirror.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fadingEdge="horizontal|vertical"
    android:theme="@style/AppTheme.AppWidgetContainer">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="Background Gradient"
        android:scaleType="fitCenter"
        android:visibility="visible"
        app:srcCompat="@drawable/widget_content_background" />

    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="Avatar"
        android:scaleType="fitCenter"
        android:visibility="visible"
        app:srcCompat="@drawable/widget_female_gogogo" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="No mood measured"
        android:textColor="@color/Font"
        android:textSize="16sp"
        android:textStyle="bold" />
</RelativeLayout>